# Delivery shared-minute windows — tasks

## Backend

- [x] `BranchDeliverySlot.bookedMinutes` + migration
- [x] `DeliverySlotService` window mode + minute reserve/list
- [x] Book delivery passes `slotMinutes` into reserve
- [x] Unit tests `tests/test_delivery_slot_minutes.py`

## Frontend

- [x] Hospital `/dashboard/delivery-slots` publish windows UI
- [x] Distributor booking shows remaining minutes + filters by need

## Docs

- [x] UX-DESIGN.md, TASKS.md
- [x] PROJECT_STATE.md
